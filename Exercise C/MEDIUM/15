//viet ham tinh tong cac chu so cua mot so nguyen
#include <stdio.h>
void sum()
{
	int num;
	printf ("Nhap mot so nguyen: ");
	scanf ("%d", &num);
	int k=0;
	while (num!=0)
	{
		int m= num%10;
		k+=m;
		num/=10;
	}
	printf ("Tong cac chu so la: %d", k);
}
int main()
{
	sum();
}
