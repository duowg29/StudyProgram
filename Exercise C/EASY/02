#include <stdio.h>
#include <math.h>
//tinh c bang a mu b
void exponent(int a, int b, int c)
{
	c=pow(a,b);
	printf ("c = %d",c);
}
int main()
{
	int a,b,c;
	printf ("Nhap so a: ");
	scanf ("%d", &a);
	printf ("Nhap so b: ");
	scanf ("%d", &b);
	exponent(a,b,c);
} 
